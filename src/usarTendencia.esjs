importar { ref } desde 'vue'

const dias = ref(1)

exportar funcion usarTendencia() {
  funcion establecerDias(value) {
    dias.value = value
  }

  funcion obtenerDias() {
    retornar dias.value
  }

  retornar {
    dias,
    establecerDias,
    obtenerDias,
  }
}
